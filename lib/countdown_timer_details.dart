import 'package:flutter/material.dart';

class CountdownTimerDetails extends StatelessWidget {
  const CountdownTimerDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}